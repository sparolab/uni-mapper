OpenLMM install components, version 1.0.0

Runtime
  Versioned OpenLMM runtime shared libraries, license and release metadata.

Development
  Public headers, static archives and CMake package/export metadata.

PluginSDK
  Toolchain-neutral C ABI v2 header and exported CMake interface target.

Plugins
  Runtime-loaded plugin entry libraries and their support shared libraries.

Tools
  Standalone open_lmm_batch executable.
